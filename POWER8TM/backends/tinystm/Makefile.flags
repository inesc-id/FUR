CFLAGS   += -w $(HTM_RETRIES) $(RETRY_POLICY) $(BATCH_RATIO)

CPPFLAGS := $(CFLAGS)
LIBS     += $(CFLAGS) -I ../../../stms/tinystm/src -lrt -mcpu=power8 -mtune=power8 -lm -L../../../stms/tinystm/lib -lpthread -lm -lstm
LDFLAGS  += $(LIBS)
